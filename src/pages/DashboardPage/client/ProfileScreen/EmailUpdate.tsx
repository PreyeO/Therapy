import EmailInfo from "@/components/screens/dashboard/client_screen/profile_ui/EmailInfo";

const EmailUpdate = () => {
  return (
    <main>
      <EmailInfo />
    </main>
  );
};

export default EmailUpdate;
